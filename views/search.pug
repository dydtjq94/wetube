extends layouts/main
include mixin/videoBlock

block content
    .search__header
        h3 Searching by #{searchingBy}
    .search__videos
        each item in videos
            +videoBlock({
                    id:item.id,
                    title:item.title,
                    views:item.views,
                    videoFile:item.videoFile,
                    })